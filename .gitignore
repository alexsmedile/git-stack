.DS_Store
**/.DS_Store
output/*
!output/.gitkeep
_backups/
_archive/
.env
.env.*

# Machine-local tool state — not part of the bundle.
# .spectacular/ itself IS tracked; only the .local/ sidecar is ignored.
.octopus/
.spectacular.local/

# Drafts, not distributed with the skill bundle
articles/
