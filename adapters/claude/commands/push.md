---
description: Commit the staged set if needed, then push through the compact git-stack preflight.
allowed-tools: Bash, Read, AskUserQuestion
argument-hint: "[commit message]"
---

# /push

Follow the **Push** fast path in the `git-ops` skill. Run it inline; do not
delegate. Use `$ARGUMENTS` as the commit message when supplied.
