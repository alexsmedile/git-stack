---
description: Close out a session — commit and push the edits so far, then optionally tag a release.
allowed-tools: Bash, Read, Edit, AskUserQuestion
argument-hint: "[version] (e.g. 1.2.0)"
---

# /wrap-up

Follow the **Wrap up a session** fast path in the `git-ops` skill. Run it
inline; do not delegate. With a version in `$ARGUMENTS`, run the release
sequence for that exact version instead.
