---
description: Commit the staged set safely through the compact git-stack preflight.
allowed-tools: Bash, Read, AskUserQuestion
argument-hint: "[commit message]"
---

# /commit

Follow the **Commit** fast path in the `git-ops` skill. Run it inline; do not
delegate. Use `$ARGUMENTS` as the commit message when supplied.

Do not push.
